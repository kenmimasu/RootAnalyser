#!/usr/bin/env python
import time,os,sys
from ROOT import TDSet,TProof,TH1F,TChain
import ROOT
# start = time.time() # Timer
# # This line loads a library to prevent a warning from coming up
# ROOT.gSystem.Load("/Users/Ken/Work/Packages/ExRootAnalysis/current/lib/libExRootAnalysis.so")
# my_file = sys.argv[1]
# new_chain = TChain(sys.argv[3]) # TREE could be 'LHCO' or 'LHEF'
# new_chain.Add(my_file)
# new_chain.Process('TPySelector',sys.argv[2]) # selector file name without '.py'
# print 'Completed in {} seconds'.format((time.time() - start))
